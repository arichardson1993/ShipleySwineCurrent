@model IEnumerable<ShipleySwine.Boar>

@{
    ViewBag.Title = "Index";
    string imgURL;
    int counter = 0;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="container">

    <div class="row row-centered">
        @foreach (var item in Model)
        {
            imgURL = "../Boars/" + item.Breed + "/" + item.NameNoSpaces + "/Thumbs/" + item.NameNoSpaces + "1.jpg";

            if (counter == 2)
            {
            @:</div>
            @:<div class="row">

                counter = 0;
            }

            <div class="col-lg-6 d-flex">
                <div class="card mb-3" style="width: 580px;">
                    <div class="row no-gutters">
                        <div class="col-md-5 flex-fill">
                            <img style="height: 238px;" src="@Url.Content(imgURL)" class="card-img" alt="Photo Didnt Load">
                        </div>
                        <div class="col-md-7 flex-fill">
                            <div class="card-body h-100">
                                <div>
                                    <div class="text-center">
                                        <h4 class="card-title">@Html.DisplayFor(modelItem => item.Name)</h4>
                                    </div>
                                </div>
                                <div class="text-center">
                                    <h6 class="card-title">Sire: @Html.DisplayFor(modelItem => item.Sire) | Dam: @Html.DisplayFor(modelItem => item.Dam)</h6>
                                </div>
                                <div class="listcontainer">
                                    @foreach (var _it in item.SellingPoints)
                                    {
                                        foreach (var _it2 in _it.SellingPoints)
                                        {
                                            <ul>
                                                <li><p class="card-text">@_it2.SellingPoint</p></li>
                                            </ul>
                                        }
                                    }
                                </div>
                                <div class="text-center">
                                    <h4>$ @Html.DisplayFor(modelItem => item.Price)</h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            counter++;
        }
    </div>
</div>

@*

        @Html.DisplayFor(modelItem => item.Farrowed)
        @Html.DisplayFor(modelItem => item.LitterSize)

        @Html.DisplayFor(modelItem => item.GuaranteedSettle)
        @Html.DisplayFor(modelItem => item.StressTest)
        @Html.DisplayFor(modelItem => item.NameNoSpaces)

        @Html.DisplayFor(modelItem => item.SireFull)

        @Html.DisplayFor(modelItem => item.DamFull)
        @Html.DisplayFor(modelItem => item.Breed)
        @Html.DisplayFor(modelItem => item.Order)
        @Html.DisplayFor(modelItem => item.Featured)
        @Html.DisplayFor(modelItem => item.FeaturedOrder)
        @Html.DisplayFor(modelItem => item.EarNotch)
        @Html.DisplayFor(modelItem => item.RegNum)
        @Html.DisplayFor(modelItem => item.TestData)
        @Html.DisplayFor(modelItem => item.BredBy)
        @Html.DisplayFor(modelItem => item.OwnedBy)
        @Html.DisplayFor(modelItem => item.Description)

    @Html.ActionLink("Edit", "Edit", new { id = item.Boar_Id }) |
    @Html.ActionLink("Details", "Details", new { id = item.Boar_Id }) |
    @Html.ActionLink("Delete", "Delete", new { id = item.Boar_Id })

*@

@*

        <div class="row">
    @foreach (var article in Model)
    {
        if (counter != 0 && counter % 3 == 0)
        {
            @:</div>
            @:<div class="row">
        }

        <div class="col-md-4">
            <div class="thumbnail">
                <img src="@article.Thumbnail" data-holder-rendered="true" style="height: 200px; width: 100%; display: block;">
                <div class="caption">
                    <h3 id="thumbnail-label">@article.Name</h3>
                    <p>@article.Description</p>
                </div>
            </div>
        </div>

        counter++;
    }

*@



