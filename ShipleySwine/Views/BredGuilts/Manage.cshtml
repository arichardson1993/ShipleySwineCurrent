@model IEnumerable<ShipleySwine.BredGuilt>
@{
    ViewBag.Title = "Manage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div style="background-color:white">

        <table class="table table-bordered table-hover">
            <thead>
                <tr class="text-center">
                    <th>Breed</th>
                    <th>Ear Notch</th>
                    <th>Price</th>
                    <th>Litter Size</th>
                    <th>Sire</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>@item.Breed</td>
                        <td>@item.EarNotch</td>
                        <td>$ @item.Price</td>
                        <td>@item.LitterSize</td>
                        <td>@item.Sire</td>
                        <td class="text-center">
                            <a style="width: 100px;" class=" btn btn-warning" href="@Url.Action("Edit","BredGuilts", new { id = item.BredGuiltId})"><i class="fa fa-pencil"> Edit</i></a>
                            <a style="width: 100px;" class=" btn btn-danger" href="@Url.Action("Delete","BredGuilts", new { id = item.BredGuiltId})"><i class="fa fa-trash"> Delete</i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
